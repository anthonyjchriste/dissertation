def mu_s_sd(n: float,
            mu_s_samp: float,
            sigma_s_samp: float,
            mu_sr: float,
            sigma_sr: float,
            mu_t_sd: float,
            sigma_t_sd: float) -> (float, float):
